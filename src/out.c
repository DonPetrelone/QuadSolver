#include <stdio.h>
#include <math.h>

//Printing results

int first(double x1) {
	char str[50];
	sprintf(str, "The first root = %lf\n", dbl);
	puts(str)

	return(0)
}

int second(double x2) {
	char str[50];
	sprintf(str, "The second root = %lf\n", dbl);
	puts(str)

	return(0)
}